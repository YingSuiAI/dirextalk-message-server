interface:
  display_name: "Direxio Contract Sync"
  short_description: "Keep public contracts and examples aligned"
  default_prompt: "Use $direxio-contract-sync when changing any Direxio HTTP route, body action, request or response schema, auth behavior, CLI example, or Postman collection."
