# v1.0.6 Release Checkpoint

- Status: in progress; explicit permission to push and publish was received.
- Updated: 2026-07-20 Asia/Shanghai.
- Repository/branch: `/home/adam/dirextalk/dirextalk-message-server`, `main`.
- Release preparation commit: `cb932b09869bd1523f52f6109fc99c2d98ebf1bd` (`chore(release): prepare v1.0.6`).
- Current `main` and `origin/main`: `4c02cf12117fab2d18c12e2ed3a21ba9a0ceef7f`, a merge that includes the release preparation plus later channel-favorite and MCP joined-room authorization fixes.

## Completed

- Prepared v1.0.6 version metadata, concise ordered release notes, and the exact tested v1.0.5 source edge (`sha256:09374d561490c327a8e038f39c4abe02c1fb49e023e61f7c644c7e47729c8201`).
- v1.0.6 includes room owner and stable member/avatar ordering fixes, channel favorites that converge through Matrix reactions, and joined-room authorization for MCP actions.
- Docker Hub inspection verified that v1.0.5 and `latest` both resolve to the same source digest above.
- Passed `bash scripts/release/contract_test.sh`, the required Go package tests, tagged upgrade test, build, formatting, and `git diff --check` before the local release commit. The contract fixture intentionally prints negative-gate cases.
- Final release-diff review found no actionable correctness or contract issues.

## Remaining

1. Commit the concise release-note update and this checkpoint, then push it to `origin/main` under the user's explicit authorization.
2. Ensure a clean tree and `HEAD == origin/main`, then run `bash scripts/release/prepare.sh v1.0.6`, `verify.sh`, and `publish.sh` with the repository release workflow.
3. Verify v1.0.6 Git tag, GitHub Release assets/checksums, embedded image version, fixed digest, trusted index entry, and fixed/`latest` digest equality.

## Do Not Disturb

- Do not bypass the release scripts or move `latest` by hand.
- Keep the v1.0.5 source digest above unchanged; it is the formal retained-data-tested upgrade identity.
