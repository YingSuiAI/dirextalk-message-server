# v1.0.4 stable release checkpoint

- Status: `complete`
- Paused: 2026-07-17 Asia/Shanghai, before WSL restart
- Target: publish `dirextalk/message-server:v1.0.4`, move `latest`, and create a lightweight GitHub Release without assets.
- Scope: simplified server release workflow and canonical server image version. The middle platform owns target and minimum client versions.

## Verified state

- Repository: `/home/adam/dirextalk/dirextalk-message-server`
- Branch: `main`
- Local and live GitHub `main`: `70a52eec3e076f0c009be5a29fcbc75c151c2a6c`
- Worktree was clean before this checkpoint file was added.
- No remote `v1.0.4` tag existed at pause time.
- Release commits:
  - `1bf22b2 chore(release): prepare v1.0.4`
  - `2c41cbc refactor(release): simplify stable publication`
  - `70a52ee fix(release): provide postgres during verification`
- First GitHub workflow run failed only because the release job had no PostgreSQL service: <https://github.com/YingSuiAI/dirextalk-message-server/actions/runs/29572497405>. Commit `70a52ee` adds the same PostgreSQL service used by CI.
- Local Docker daemon was unresponsive even though the service reported active. The user is restarting WSL.

## Completed checks

- `bash scripts/release/contract_test.sh` passed after the PostgreSQL workflow fix.
- `git diff --check` passed.
- Focused Go release/server checks passed before the formal release attempt.
- Flutter updater/version checks passed: 42 tests across `app_version_catalog_test.dart`, `release_controller_test.dart`, and `server_update_reminder_test.dart`; Flutter production code was unchanged.
- Compatibility result: no client-incompatible server code change was found. The new release actions are additive and canonical `vX.Y.Z` is injected into formal Docker builds.

## Resume order

1. Verify `docker version` shows both Client and Server after WSL restart.
2. Verify live GitHub `main` is still `70a52ee` and the worktree contains no unrelated changes.
3. Trigger `release.yml` with `version=v1.0.4` and monitor it to completion.
4. Verify the immutable `v1.0.4` image and `latest` resolve to the published digest, the image reports `v1.0.4`, the annotated Git tag points to `70a52ee`, and the GitHub Release has zero assets.
5. Mark this checkpoint complete and report release status plus: `客户端不兼容代码：没有`.

## Risks and boundaries

- The locally inspected `/home/adam/dirextalk/dirextalk-updater` source still expected old GitHub release metadata. Do not restore those removed assets; the user explicitly selected the middle-platform catalog flow. Confirm the deployed updater matches that new contract separately if needed.
- Do not disturb ignored `.release/v1.0.4` evidence/context or unrelated user work.

## Completion

- Completed: 2026-07-17 Asia/Shanghai
- Released source commit: `70a52eec3e076f0c009be5a29fcbc75c151c2a6c`
- Docker image: `dirextalk/message-server:v1.0.4`
- Immutable digest: `sha256:da89039e01fc6b6f55e1e599baa647b95337a0a864d53d16d155ffc258d75401`
- `latest` independently resolved to the same digest.
- The published image reported exact version `v1.0.4`.
- Annotated tag `v1.0.4` peeled to the released source commit.
- GitHub Release: <https://github.com/YingSuiAI/dirextalk-message-server/releases/tag/v1.0.4>, stable and published with zero assets.
