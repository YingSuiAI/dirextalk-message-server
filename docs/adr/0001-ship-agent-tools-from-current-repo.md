# Ship Agent Tools From the Current Repository

The first version of Direxio agent tooling will live in this repository as an independent tool surface rather than a separate repository or embedded monolith subsystem. This keeps the CLI and MCP adapters close to the live P2P action catalog, Agent token permissions, Postman examples, and API change record while the contract is still evolving.
