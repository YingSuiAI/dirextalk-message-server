# Use a Domain Command Tree for the Agent CLI

The Direxio CLI will expose a domain command tree for common P2P and Matrix workflows, including auth, initialization, contacts, channels, groups, Matrix session initialization, message send/list, sync, and listen. `direxio p2p action` remains available as a fallback for direct body-action calls that are not yet represented by a first-class command.
