# Use CLI-First Agent Integration

Dirextalk's first agent integration surface will be CLI-first, with agent skills or recipes documenting how intelligent agent tools should invoke it. MCP will remain a thin optional adapter for a curated set of safe, AI-friendly workflows rather than a full mirror of every P2P action.
