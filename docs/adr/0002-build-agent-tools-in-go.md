# Build Agent Tools in Go

Direxio's first-party agent tools will be implemented in Go and released as cross-platform binaries. This keeps the toolchain aligned with the monolith repository, allows CLI and MCP adapters to share a typed P2P client, and avoids requiring Developer Operators to install a language runtime before using the tools.
